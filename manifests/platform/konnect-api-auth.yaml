apiVersion: konnect.konghq.com/v1alpha1
kind: KonnectAPIAuthConfiguration
metadata:
  name: konnect-api-auth
  namespace: kong
spec:
  type: secretRef
  # The token value comes from the Kubernetes Secret `konnect-token`,
  # created by the CI workflow from the GitHub Actions secret KONNECT_TOKEN.
  # The secret must carry the label `konghq.com/credential=konnect` and
  # store the token under the `token` key.
  secretRef:
    name: konnect-token
  serverURL: au.api.konghq.com
